Place the packaged browser driver(s) here for production desktop builds.

Supported filenames:
- chromedriver.exe
- msedgedriver.exe

If browser driver executables are not present, runtime fallback will use Selenium Manager or webdriver-manager to resolve the correct driver.

Electron packaging copies this directory into:
resources/backend/drivers/
